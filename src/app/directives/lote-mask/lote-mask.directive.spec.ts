import { LoteMaskDirective } from './lote-mask.directive';

describe('LoteMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new LoteMaskDirective();
    expect(directive).toBeTruthy();
  });
});
