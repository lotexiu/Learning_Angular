import { WritterAnimationDirective } from './writter-animation.directive';

describe('WritterAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new WritterAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
